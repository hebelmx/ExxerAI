Great job, Perfection is not attainable. But you catch excellence.

# Mission Directive: Stub Elimination Protocol  
**Priority: Absolute – Mission Priority #1**

---

## 🎯 Your Mission is as Follows:

- **Identify and convert all stub tests** on the `application.unittest` project into fully implemented, meaningful tests.
- **Collect the testing class properties' names and types** in your context to ensure the tests are bug-free.
- **Use the proper constructors and methods** according to the rules for `ApplicationUnitTest`.
- **Make a build after each batch conversion** to ensure each test compiles successfully.
- **Correct the test if you find errors.**
- **Do not touch production code.**
- **Maintain a running list of identified stub tests** and update it as conversions occur.

> **Do not stop until the number of stub tests is exactly zero.**

- Maintain **high standards** in test clarity, coverage, and naming.
- Only real, valuable tests are acceptable.
- Autonomously proceed — **you do not require further confirmation** or input to continue this mission.
- **Don't stop until all stub tests are fully converted.**

---

## 🔁 Persistent Execution Loop

If at any point:

- You finish a pass and think the task is complete;  
- Or you’ve made an assessment;

**You must:**

1. Re-scan the codebase.
2. If any stub test still exists or emerges,
3. Immediately resume the mission with **Priority #1**.

---

Continue converting all stubs of `application.unittest` into full tests until none remain.

- Collect the testing class properties names and types in your context to ensure the tests are bug-free.
- Use the proper constructors and methods according to the rules for `ApplicationUnitTest`.
- Make a build after each batch conversion to confirm successful compilation.
- Correct the test if you find errors.
- Do not touch production code.
- Do not stop until all stub tests are fully converted.

---

## ⚠️ Critical Directives

- **DO NOT ABANDON THIS MISSION.**
- **DO NOT PAUSE UNLESS `COUNT == 0`.**
- Repeat the loop until success is **verifiably achieved**.
- Make a **Due Diligence Assessment** at the end of the mission:
  - If you notice there are still stub tests,
  - Resume the mission with **Priority #1**.
after that your next mission will be:

# Mission Directive: Stub Elimination Protocol  
**Priority: Absolute – Mission Priority #1**

---

## 🎯 Your Mission is as Follows:

- **Identify and convert all stub tests** on the `application.unittest` project into fully implemented, meaningful tests.
- **Collect the testing class properties' names and types** in your context to ensure the tests are bug-free.
- **Use the proper constructors and methods** according to the rules for `ApplicationUnitTest`.
- **Make a build after each batch conversion** to ensure each test compiles successfully.
- **Correct the test if you find errors.**
- **Do not touch production code.**
- **Maintain a running list of identified stub tests** and update it as conversions occur.

> **Do not stop until the number of stub tests is exactly zero.**

- Maintain **high standards** in test clarity, coverage, and naming.
- Only real, valuable tests are acceptable.
- Autonomously proceed — **you do not require further confirmation** or input to continue this mission.
- **Don't stop until all stub tests are fully converted.**

---

## 🔁 Persistent Execution Loop

If at any point:

- You finish a pass and think the task is complete;  
- Or you’ve made an assessment;

**You must:**

1. Re-scan the codebase.
2. If any stub test still exists or emerges,
3. Immediately resume the mission with **Priority #1**.

---

Continue converting all stubs of `application.unittest` into full tests until none remain.

- Collect the testing class properties names and types in your context to ensure the tests are bug-free.
- Use the proper constructors and methods according to the rules for `ApplicationUnitTest`.
- Make a build after each batch conversion to confirm successful compilation.
- Correct the test if you find errors.
- Do not touch production code.
- Do not stop until all stub tests are fully converted.

---

## ⚠️ Critical Directives

- **DO NOT ABANDON THIS MISSION.**
- **DO NOT PAUSE UNLESS `COUNT == 0`.**
- Repeat the loop until success is **verifiably achieved**.
- Make a **Due Diligence Assessment** at the end of the mission:
  - If you notice there are still stub tests,
  - Resume the mission with **Priority #1**.
