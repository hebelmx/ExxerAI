# Test Dependencies for MCP Server Collection
# Core testing (built into Python)
# unittest - built-in

# Optional dependencies for enhanced testing
coverage>=7.0.0     # Code coverage reporting
pytest>=7.0.0       # Alternative test runner (optional)
pytest-asyncio>=0.21.0  # For async test support in pytest

# Production dependencies (needed for tests to import modules)
flask>=2.3.0        # For HTTP server functionality
requests>=2.31.0    # For discovery and HTTP client functionality  
psutil>=5.9.0       # For system monitoring functionality
